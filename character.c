#include	<stdio.h>
/**
 * main	-	Description:	Entry	point
 *
 * Return:	0	(success)
 *
 */

int	main(void)	/*	Return	void	*/

{

	char	UPPERCASE;	=	()
	char	LOWERCASE;	=	()
	putchar((UPPERCASE)	(LOWERCASE));

		if(UPPERCASE	==	'A'	&&	<=	'Z');
		{
			printf("%c",	(UPPERCASE));
			putchar(UPPERCASE);
		}
		if(LOWERCASE	==	'a'	&&	<=	'z');
		{
			printf("%c",	(LOWERCASE));
			putchar(LOWERCASE);
		}
}

